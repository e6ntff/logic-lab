import { EdgeTypes, NodeTypes } from 'reactflow';
import And from '../components/And';
import Wire from '../components/Wire';

export const nodeTypes: NodeTypes = { and: And };

export const edgeTypes: EdgeTypes = { wire: Wire };
